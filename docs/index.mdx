---
title: Overview
description: Choose the n3wth tool that matches your task.
---

These guides cover the developer tools maintained in the n3wth workspace.
Start with the product that matches your task. You do not need to install them all.

This hub documents the public contracts of the workspace. Each guide links to the source of truth so you can verify examples before shipping them.

<CardGroup cols={3}>
  <Card title="UI" href="/ui/quickstart">
    Build a React site with shared primitives, themes, and layouts.
  </Card>

  <Card title="Skills" href="/skills/quickstart">
    Give an agent repeatable, inspectable task instructions.
  </Card>

  <Card title="r3" href="/r3/quickstart">
    Keep useful context across agent sessions through MCP.
  </Card>
</CardGroup>

## Pick UI or Kit

**UI** is a dependency. Upgrade the package to receive shared component changes.
Use it when you want the same visual foundation across applications.

**Kit** distributes source files. You can change those files without changing
the registry. Use it when your application needs to own its components.

Both expose React UI, but their installation and update contracts differ.
Follow the relevant guide. Do not mix setup snippets from the two.

<Tip>
  If you are unsure where to start, use UI for an existing React application.
  Use Skills when the deliverable is an assistant workflow, not runtime code.
</Tip>

## Start with one working example

Each product section starts with prerequisites and a small first task. It then
explains configuration, reference material, and failures you can diagnose.
Examples describe the source in `n3wth/n3wth`. The exception is r3's runtime,
which is maintained separately in `n3wth/r3`.

For changes to this repository, use the [workspace guide](/workspace).
For hosting these pages, see [publishing the documentation](/publishing).

## Related projects

- [Lunch Money for Agents](https://lunchmoney.sh): a separate read-only financial
  MCP integration with its own [setup guide](https://github.com/n3wth/lunchmoney-mcp).
- [Canvas](https://github.com/n3wth/canvas): live canvases and realtime sync.
- [Markup](https://github.com/n3wth/markup): an independent prototype for agents
  working with people in documents and chat.

Those projects have separate source and release boundaries. This hub links to
their repositories. It does not imply that their runtimes ship in this workspace.
