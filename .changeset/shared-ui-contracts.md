---
"@n3wth/ui": patch
---

Synchronize theme hook callers and keep automatic system changes separate from saved user preferences. Preserve client behavior in emitted package modules and verify public exports through standalone consumers.

Remove excluded commercial font rules from release CSS so external consumers use the documented system-font fallback without unresolved asset imports.
