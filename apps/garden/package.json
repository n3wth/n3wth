{
  "name": "@n3wth/garden",
  "private": true,
  "type": "module",
  "version": "0.1.2",
  "engines": { "node": "24.x" },
  "scripts": {
    "build": "node scripts/build-redirects.mjs",
    "check": "node --test scripts/redirects.test.mjs && npm run build",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "typecheck": "node --check worker.mjs"
  }
}
